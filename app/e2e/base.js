/* eslint-disable no-undef */

// ui测试基础配置

// 超时时间
const TIMEOUT = global.__TIMEOUT__
// 日志文件夹路径
const LOG_DIR = global.__LOG_DIR__
const URL_PREFIX = global.__URL_PREFIX__

export {
  TIMEOUT,
  LOG_DIR,
  URL_PREFIX,
}